# JWT Authentication System using GraphQL

- In this prject I have implemented a json web token(jwt) authentication system using graphql

- To run this project clone the repository and do `npm install`

